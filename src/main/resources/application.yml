server:
  port: 8081

spring:
  application:
    name: AdminPanel

  datasource:
    url: jdbc:postgresql://localhost/admin_panel
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

#logging:
  #level:
    #org.springframework.web: ERROR
    #eva.examle: DEBAG
  #pattern:
    #console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"